# `src/` — código de produção

Lógica central da aplicação, isolada de ferramentas externas e orquestradores. Organize conforme
`docs/standards/architecture.md` (ex.: `domain/`, `utils/`). É o código que os testes em `test/`
validam no ciclo TDD.

Este README é só um marcador da pasta; substitua-o pelo código real do projeto.
